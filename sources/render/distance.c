#include "../../inc/Cube3D.h"

// float get_shortest_distance_to_wall(t_mlx *mlx, t_rays *ray, t_map *map, float x, float y, float angle) 
// {
//     float horizontal_distance = rays_horizontal(mlx, ray, map, x, y, angle);
//     float vertical_distance = rays_vertical(mlx, ray, map, x, y, angle);
//     if(horizontal_distance < vertical_distance) {
//         return horizontal_distance;
//     } else {
//         return vertical_distance;
//     }
// }
